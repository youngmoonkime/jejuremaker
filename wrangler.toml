name = "jejuremaker"
compatibility_date = "2024-02-02"
pages_build_output_dir = "dist"

# R2 Bucket Binding
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "jeju-remaker-assets"
preview_bucket_name = "jeju-remaker-assets" # Optional: Use same bucket for preview or separate one

[vars]
# Environment variables for the Worker/Function
# Note: Secrets like TRIPO_API_KEY should be set via 'wrangler pages secret put' or in the Dashboard
